@import "html5-reset"

// Linear gradients
linearGradient(startPoint, startColor, startInterval, endColor, endInterval)
    background-color rgba(startColor)
    background-image -webkit-linear-gradient(startPoint, startColor startInterval, endColor endInterval)
    background-image -moz-linear-gradient(startPoint, startColor startInterval, endColor endInterval)
    background-image -ms-linear-gradient(startPoint, startColor startInterval, endColor endInterval)
    background-image -o-linear-gradient(startPoint, startColor startInterval, endColor endInterval)
    background-image linear-gradient(startPoint, startColor startInterval, endColor endInterval)

// Bright outline so you can see stuff
blueprint(color= cyan)
    border 1px dashed color

// Vendor function to output vendor prefixes
vendor(thing, value)
  for vendor in vendors
    if vendor == official
      {thing} value
    else
      -{vendor}- {thing} value

// Transparent function for border radius
border-radius()
    vendor(border-radius, arguments)

// Micro ClearFix hack
cf()
    *zoom 1
    &:before, &:after
        content ""
        display table
    &:after
        clear both

// -9999px replacement hack
indentOutOfSight()
    text-indent 100%
    white-space nowrap
    overflow hidden
    color transparent 

// Colour functions to provide solid colour fallbacks for old browsers
background-color()
    if arguments == transparent
       background-color transparent
    else if alpha(arguments) != 1
        background-color rgb(arguments)
        background-color rgba(arguments)
     
    else
        background-color arguments
color()
    if arguments == transparent
       color transparent
    else if alpha(arguments) != 1
        color rgb(arguments)
        color rgba(arguments)
    else
        color arguments

// Some CSS3 modifiers
transform()
    vendor(transform, arguments)
transition(what = all, duration = 200ms, timing = ease)
    vendor(transition, what duration ease)


*
    box-sizing border-box


$column = 100%/18

// STYLES BEGIN!


.pull-right
    float right


html
body
    font-family 'Avenir Next'
    background-color rgba(38,41,44,1)
    color rgba(246,246,246,1)
    margin 1em $column

.button
button
    display inline-block
    border none
    text-decoration none
    background-color rgba(75,79,83,1)
    color rgba(246,246,246,1)
    font-family "Avenir Next Condensed"
    font-size 0.9em
    padding 0.25em 0.5em
    margin 0
    text-transform uppercase
    line-height 2em
    cursor pointer
    vertical-align middle
    &:active
        color rgba(white, 0.8)
        background-color rgba(64,117,171,1)

.toolbar
    margin 1em 0

.bluggr__title
    font-weight 300
    font-size 2em
    text-transform capitalize
    margin-bottom 1em

.bluggr__list
    font-size 1.1em
    margin-top 1em
    list-style none
    li
        cursor pointer
        color #999
        margin-bottom 0.5em
        background-color rgba(75,79,83,0.25)
        padding 0.5em
        border 1px solid transparent
        transition();
        &:hover
            border 1px solid rgba(98,144,169,0.25)
        &:active
            opacity 0.5
        a
            text-decoration none
            display block
            color white
            big
                color rgba(246,132,55,1)
            small
                color rgba(114,170,202,1)
                font-size 80%


.section-wrap
    .title
        display block
        color rgba(75,79,83,1)

.split50
    width 50%
    float left

.preview
    padding 0.25em

.editor
.editor--field
    padding 0.25em
    font-family 'Cousine'
    font-size 1.2em
    min-height 10em
    width 90%
    max-width 90%
    border none
    background-color rgba(73,76,80,1)
    color rgba(184,217,103,1)
.editor--field
    min-height 1em
    width 90%


.unsaved
    opacity 0
.saved
    color rgba(184,217,103,1)


.preview
    h1
    h2
    h3
        margin 0.5em 0
        font-weight 400

    h1
        font-size 2.5em
        font-weight 200
        color rgba(246,132,55,1)
    h2
        font-size 1.5em
        text-transform uppercase
        color rgba(107,167,202,1)
        font-weight 200
    h3
        font-size 1.15em
        color rgba(107,167,202,1)

    p
        margin 0.25em 0
        margin-bottom 0.75em
        color rgba(111,170,202,1)